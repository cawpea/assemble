<header>
    <div>header.hbs</div>
    <h1>{{config.siteName}}</h1>
    <nav>
        <ul>
        {{#each config.siteMenu}}
            <li><a href="{{url}}">{{title}}</a></li>
        {{/each}}
        </ul>
    </nav>
</header>
